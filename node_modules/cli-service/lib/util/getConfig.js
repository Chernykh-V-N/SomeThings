module.exports = function getConfig(key) {
  const config = process.PROJECT_CONFIG[process.env.NODE_ENV]

  if (config && config[key]) {
    return config[key]
  } else {
    return process.PROJECT_CONFIG.common[key]
  }
}